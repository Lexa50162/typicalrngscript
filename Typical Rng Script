local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "AlexHub | typical.rng script",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "TYPICAL RNG SCRIPT",
   LoadingSubtitle = "by Alex",
   Theme = "Ocean", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "are you a star",
      Subtitle = "are you?",
      Note = "hint yes", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"yes"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})


local Tab = Window:CreateTab("Main", 4483362458) -- Title, Image




   
local Button = Tab:CreateButton({
   Name = "click all sans",
   Callback = function()
   _G.AutoClickSanses = true

function autoclickbosses()
    while _G.AutoClickSanses do
        for i,clickd in pairs(workspace:GetDescendants()) do
            if clickd:IsA("ClickDetector") and clickd.Parent.Name == "ClickHitbox" then
                fireclickdetector(clickd)
            end
        end
        task.wait(0.025)
    end
end

autoclickbosses()
   end,
})



local Button = Tab:CreateButton({
   Name = "no cd tracking blaster keep clicking",
   Callback = function()
   local args = {
	vector.create(-59.283287048339844, 3.699151039123535, 111.76821899414062)
}
game:GetService("Players").LocalPlayer:WaitForChild("Backpack"):WaitForChild("Tracking Blaster"):WaitForChild("RemoteEvent"):FireServer(unpack(args))

   end,
})



local Slider = Tab:CreateSlider({
   Name = "Walkspeed",
   Range = {16, 694.20},
   Increment = 10,
   Suffix = "Speed",
   CurrentValue = 1,
   Flag = "Slider", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value 
   end,
})


local Button = Tab:CreateButton({
   Name = "godmode (aint made by me)",
   Callback = function()
loadstring(game:HttpGet('https://raw.githubusercontent.com/3dsonsuce/acrylix/main/Acrylix'))()

   end,
})



local EdAutoFarm = false
local RunService = game:GetService("RunService")
local connection

local Toggle = Tab:CreateToggle({
    Name = "Ed AutoFarm (recommended to use godmode)",
    CurrentValue = false,
    Flag = "EdAutoFarmToggle",
    Callback = function(Value)
        EdAutoFarm = Value

        if EdAutoFarm then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-217.936874, 6.69915056, 128.66214, 0.649448216, 0, -0.760405838, 0, 1, 0, 0.760405838, 0, 0.649448216)

            connection = RunService.Heartbeat:Connect(function()
                local args = {
                    Vector3.new(-163.98483276367188, 3.699151039123535, 85.7881088256836)
                }
                local tool = game.Players.LocalPlayer:FindFirstChild("Backpack"):FindFirstChild("ERRORDUST BLASTER")
                if tool then
                    tool:FindFirstChild("RemoteEvent"):FireServer(unpack(args))
                end
            end)
        else
            if connection then
                connection:Disconnect()
                connection = nil
            end
        end
    end,
})




local Section = Tab:CreateSection("Credits")


local Divider = Tab:CreateDivider()



local Label = Tab:CreateLabel("DistortionAlt (did ed autofarm toggle pro", "13742592984")
